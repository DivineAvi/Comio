# ╔══════════════════════════════════════════════════════════╗
# ║  Comio — Environment Variables                           ║
# ║  Copy this file: cp .env.example .env                    ║
# ║  Then fill in your real values below.                    ║
# ╚══════════════════════════════════════════════════════════╝

# ── App ────────────────────────────────────────────────────
APP_NAME=Comio API
APP_VERSION=0.1.0
DEBUG=true

# ── Server ─────────────────────────────────────────────────
HOST=0.0.0.0
PORT=8000

# ── Database ───────────────────────────────────────────────
# Format: postgresql+asyncpg://user:password@host:port/dbname
# The default matches the docker-compose.yml Postgres config
DATABASE_URL=postgresql+asyncpg://comio:comio@localhost:5432/comio

# ── Redis ──────────────────────────────────────────────────
REDIS_URL=redis://localhost:6379/0

# ── Auth (JWT) ─────────────────────────────────────────────
# IMPORTANT: Generate a real secret for production!
# Run: python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET_KEY=change-me-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60

# ── CORS ───────────────────────────────────────────────────
# Comma-separated list of allowed frontend origins
CORS_ORIGINS=["http://localhost:3000"]

# ── Docker / Sandbox ──────────────────────────────────────
DOCKER_HOST=unix:///var/run/docker.sock
SANDBOX_NETWORK=comio-sandbox
SANDBOX_IMAGE=comio/sandbox:latest

# ── GitHub ─────────────────────────────────────────────────
# Personal access token (for repo access, PR creation)
# Create at: https://github.com/settings/tokens
GITHUB_TOKEN=
# OAuth App credentials (for "Sign in with GitHub")
# Create at: https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ── Deployment ─────────────────────────────────────────────
DEPLOY_DOMAIN=comio.dev
ARTIFACT_REGISTRY=
GKE_CLUSTER=
GKE_ZONE=us-central1-a
GCP_PROJECT_ID=

# ── LLM Providers ─────────────────────────────────────────
# Get your API key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=
# Get your API key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=
# Which provider and model to use by default
DEFAULT_LLM_PROVIDER=openai
DEFAULT_LLM_MODEL=gpt-4o